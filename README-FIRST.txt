SEEKVERA FINAL BUILD
This package is a complete replacement-ready static build.
It contains the homepage, all 10 working department pages, shared JS/CSS, legal pages,
404 page, favicon, manifest, Cloudflare wrangler config, robots, sitemap and existing verification files.

Important:
- Search routes are live external provider/search routes.
- Smart routing is zero-cost browser logic, not a paid external AI model.
- Unknown requests fall back to General Search instead of a wrong department.
- No fake affiliate/tracking IDs are included. Add only real approved partner links.
