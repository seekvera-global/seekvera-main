name: R32 superseded by R35

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/r32-static-final-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: seekvera-r32-static-final
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R32 serial audit is superseded by R35 parallel 98-language final audit.'
