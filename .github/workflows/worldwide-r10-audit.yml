name: Legacy SEEKVERA worldwide locale audit
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by SEEKVERA R23 final release audit.'
