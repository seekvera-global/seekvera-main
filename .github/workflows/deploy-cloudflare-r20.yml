name: Legacy SEEKVERA Cloudflare deploy (R20)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by the R23 production deploy workflow.'
